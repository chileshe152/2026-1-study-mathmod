---
## Author
author:
  name: Чилеше Лупупа
  email: 1032225194@rudn.ru
  affiliation:
    - name: Российский университет дружбы народов
      country: Российская Федерация
      postal-code: 117198
      city: Москва
      address: ул. Миклухо-Маклая, д. 6

## Title
title: "Математическое моделирование"
subtitle: "Лабораторная работа № 3"
license: "CC BY"
date: today
date-format: "YYYY-MM-DD"
---

# Вводная часть

## Цель работы

Изучить модели боевых действий Ланчестера и проанализировать, как изменяется численность противоборствующих сторон при различных типах конфликта.

## Задание

1. Рассмотреть три варианта моделей Ланчестера.
2. Построить графики изменения численности войск.
3. Проанализировать динамику решений и определить исход противостояния.

# Теория: модели боевых действий

## Общая идея моделей Ланчестера

В модели участвуют две противоборствующие стороны, численности которых задаются функциями

$$
x(t), \quad y(t)
$$

Если в некоторый момент одна из численностей становится равной нулю, соответствующая сторона считается проигравшей.

## Случай 1: регулярные войска против регулярных войск

Математическая модель имеет вид:

$$
\begin{cases}
\frac{dx}{dt}= -a(t)x(t) - b(t)y(t) + P(t) \\
\frac{dy}{dt}= -c(t)x(t) - h(t)y(t) + Q(t)
\end{cases}
$$

В этой системе учитываются:

- потери, не связанные непосредственно с боевыми действиями;
- боевые потери от воздействия противника;
- поступление подкреплений.

## Случай 2: регулярные войска против партизан

Если одна из сторон действует партизанскими методами, модель принимает вид:

$$
\begin{cases}
\frac{dx}{dt}= -a(t)x(t) - b(t)y(t) + P(t) \\
\frac{dy}{dt}= -c(t)x(t)y(t) - h(t)y(t) + Q(t)
\end{cases}
$$

Здесь потери нерегулярных сил определяются не только численностью регулярной армии, но и численностью самих партизан.

## Случай 3: партизаны против партизан

При противостоянии двух партизанских формирований система записывается следующим образом:

$$
\begin{cases}
\frac{dx}{dt}= -a(t)x(t) - b(t)x(t)y(t) + P(t) \\
\frac{dy}{dt}= -h(t)y(t) - c(t)x(t)y(t) + Q(t)
\end{cases}
$$

В этом случае взаимодействие обеих сторон описывается нелинейными членами.

# Упрощённые модели

## Жесткая модель для регулярных армий

Если не учитывать подкрепления и небоевые потери, система упрощается до вида:

$$
\begin{cases}
\frac{dx}{dt}= -by \\
\frac{dy}{dt}= -ax
\end{cases}
$$

Для такой модели можно получить точное аналитическое решение, а фазовые траектории имеют вид гипербол.

## Качественный вывод

Исход конфликта определяется не только начальной численностью сторон, но и эффективностью их вооружения и боевых действий.

Основные выводы:

- численное превосходство даёт заметное преимущество;
- для компенсации этого преимущества требуется существенно более высокая эффективность.

# Постановка задачи

## Исходные данные

Между странами $X$ и $Y$ ведутся боевые действия.

Начальные численности армий составляют:

$$
x(0)=32888, \qquad y(0)=17777
$$

Требуется исследовать изменение численности войск для двух моделей.

## Случай 1: линейная модель

$$
\begin{cases}
\frac{dx}{dt}= -0.55x(t) - 0.77y(t) + 1.5\sin(3t+1) \\
\frac{dy}{dt}= -0.66x(t) - 0.44y(t) + 1.2\cos(t+1)
\end{cases}
$$

## Случай 2: нелинейная модель

$$
\begin{cases}
\frac{dx}{dt}= -0.27x(t) - 0.88y(t) + \sin(20t) \\
\frac{dy}{dt}= -0.68x(t)y(t) - 0.37y(t) + \cos(10t)
\end{cases}
$$

# Эксперимент: базовые расчёты

## Базовый эксперимент: линейная модель

![](image/single_experiment_linear.png)

## Базовый эксперимент: линейная модель

По результатам моделирования видно следующее:

- обе функции уменьшаются с течением времени;
- переменная $x(t)$ снижается постепенно;
- переменная $y(t)$ убывает интенсивнее и к концу интервала почти обращается в нуль;
- динамика системы имеет устойчивый и предсказуемый характер.

## Базовый эксперимент: нелинейная модель

![](image/single_experiment_nonlinear.png)

## Базовый эксперимент: нелинейная модель

Для нелинейной модели наблюдается иная картина:

- функция $x(t)$ уменьшается медленнее, чем в линейном случае;
- функция $y(t)$ очень быстро стремится к нулю;
- далее поведение системы определяется главным образом изменением $x(t)$;
- нелинейные слагаемые резко усиливают затухание одной из переменных.

# Параметрический анализ

## Сканирование траекторий $x(t)$

![](image/parametric_scan_x_comparison.png)

## Сканирование траекторий $x(t)$

В ходе исследования изменялись параметры модели.

Полученные результаты показывают:

- рост параметра приводит к ускорению убывания $x(t)$;
- траектории становятся более крутыми;
- различия между решениями особенно заметны на средних и больших временах.

## Сканирование траекторий $y(t)$

![](image/parametric_scan_y_comparison.png)

## Сканирование траекторий $y(t)$

Анализ графиков позволяет сделать следующие выводы:

- в линейной модели увеличение параметра ускоряет снижение $y(t)$;
- в нелинейной модели $y(t)$ почти сразу обращается в нуль;
- влияние параметра во втором случае выражено слабо, так как доминируют нелинейные эффекты.

# Анализ вычислительных затрат

## Время вычислений

![](image/computation_time_vs_parameter.png)

## Время вычислений

Результаты численного эксперимента показывают:

- линейная модель требует меньше вычислительных затрат;
- время расчёта линейной системы имеет порядок $10^{-5}$ сек;
- для нелинейной системы характерны значения порядка $10^{-4}$–$10^{-3}$ сек;
- даже в более сложном случае вычисления выполняются очень быстро.

# Анализ итоговой метрики

## Метрика norm_final

В качестве итоговой характеристики использовалась величина

$$
\text{norm\_final}=\sqrt{x(t_{final})^2 + y(t_{final})^2}
$$

Эта метрика характеризует общее состояние системы в конце интервала моделирования.

## Зависимость norm_final от параметра

![](image/norm_final_vs_parameter.png)

## Интерпретация результата

По графику можно сделать следующие выводы:

- с увеличением параметра значение метрики уменьшается;
- линейная система быстрее приближается к состоянию покоя;
- нелинейная система дольше сохраняет заметное значение итоговой нормы;
- это связано с более медленным затуханием переменной $x(t)$.

# Итоги

## Выводы

1. Линейная модель характеризуется плавным и устойчивым уменьшением обеих переменных.
2. В нелинейной модели переменная $y(t)$ практически сразу стремится к нулю.
3. Изменение параметров существенно влияет на скорость затухания решений.
4. Для линейной модели это влияние проявляется более явно.
5. Нелинейная система требует больших вычислительных затрат, но остаётся удобной для численного анализа.
6. Метрика $\text{norm\_final}$ убывает при увеличении параметра, что указывает на усиление затухания динамики.

# Список литературы

1. Законы Осипова — Ланчестера.
2. Дифференциальные уравнения динамики боя.
3. Элементарные модели боя.
